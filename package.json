{
  "name": "itsy-bitsy-spider-home-row",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "A self-hostable Three.js typing game that teaches home row through an Itsy Bitsy Spider climb.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "deploy:dry-run": "npm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "three": "^0.185.0",
    "tone": "^15.1.22"
  },
  "devDependencies": {
    "vite": "^8.1.0",
    "wrangler": "^4.105.0"
  }
}
